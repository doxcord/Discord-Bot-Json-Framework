const cmds = require('../commands')

new Handler ({
  for(ping in cmds) {
    cmds = ['Framework']
    if(Framework == true) {
      cmds.push()
    }
});
 
/**
_______________NOTE______________________
               ^^^^
/* This is a unfished command handler */
  
/**
